// The seal is part of the ATHAR logo system (see ./Logo). Re-exported here so
// existing `import { Seal } from "../ui/Seal"` keeps working with the new mark.
export { AtharSeal as Seal } from "./Logo";
